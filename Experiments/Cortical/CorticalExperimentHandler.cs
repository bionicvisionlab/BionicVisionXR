/* public Electorde[] firstStim, secondStim;  

void Update(){
    if(Input.GetKeyDown(Keycode.Return)
        OptimizeElectrodes(); 
        
    if( Input == keycode1{
        BackendShaderHandler.Instance.electrodesBuffer.SetData(firstStim)
        
    if( Input = 2)
        do the other one.
        */  